3D TPS 关卡 Demo
项目简介
独立开发的 Unity 第三人称射击单关卡原型，围绕「移动射击 → 资源拾取 → 开门解谜 → 尸潮战斗 → 存档撤离」搭建完整可玩循环；
---
 技术栈
 UI ： UGUI、TextMeshPro
 输入 ： Input System
 角色基础 ： Starter Assets 第三人称控制器、Cinemachine 目标点跟随相机
 动画 ： Animator Controller、Blend Tree、多层动画、动画事件转发
 战斗检测 ： `Physics.Raycast` 命中检测（Hitscan 无真实弹道）
 导航 ： NavMesh 烘焙、NavMeshAgent 追击
 数据配置 ： ScriptableObject（武器参数、音频目录 SO）
 持久化 ： `JsonUtility` + `Application.persistentDataPath`，5 槽位本地 JSON
 架构模式 ： 接口多态（可受伤目标）、`EventCenter` 与 C# 事件、玩家状态收口、敌人显式状态机
 性能基础设施 ： 通用对象池、一次性 3D 音效池

---

 核心脚本 / 模块说明

 1. 战斗与多武器系统

 从准星方向用射线做 Hitscan，命中后由目标上「可受伤」接口统一处理，射击层不关心是僵尸、靶子还是可破坏物。武器架管理列表与数字键/空手切换；伤害、弹匣、散布等由武器配置 SO 注入射击组件，换枪只换数据、不改核心射击逻辑。

> 脚本：`HitscanShooter.cs`、`WeaponHolder.cs`、`WeaponDataSO.cs`、`AmmoType.cs`

 2. 可受伤接口与伤害链

 定义统一受伤入口，僵尸血条、练习靶等各自实现；射击侧只做组件查询与调用，不写目标类型分支。新可击打体只需实现接口即可接入。

> 脚本：`IDamageable.cs`、`ZombieHealth.cs`、`DamageableTarget.cs`、`PlayerHealth.cs`

 3. 敌人 AI 与状态机

 `NavMeshAgent` 做寻路位移；追击/攻击由追击 AI 与僵尸状态机协同；桥接脚本把速度映射到 Animator。死亡时停用寻路、进死亡状态、延迟销毁，寻路/动画/血量分脚本维护。

> 脚本：`ZombieChaseAI.cs`、`ZombieStateMachine.cs`、`IZombieState.cs`、`ZombieAttack.cs`、`ZombieHealth.cs`、`ZombieAnimationBridge.cs`

 4. 玩家状态管理与动画同步

 对现有瞄准、射击、背包等组件不强行重写，在 LateUpdate 末尾把多源状态收拢为统一枚举，对外一个观察口；动画通过多条桥接脚本（位移/持枪层/空手切换/事件转发）分责，降低侵入、便于再收敛成唯一状态机。

> 脚本：`PlayerStateManager.cs`、`PlayerState.cs`、`PlayerFsmConcreteStates.cs`、`PlayerAnimationBridge.cs`、`PlayerGunAnimatorBridge.cs`、`PlayerUnarmedLocomotionSwitcher.cs`、`PlayerLocomotionAnimationEventRelay.cs`

 5. 背包、拾取与医疗

 运行时格档数据在背包中管理，与只读配置隔离。背包 UI 用 UI 拖拽接口实现格间交换；右键根据物品类型走医疗或纸条阅读。医疗在独立动作里做满血/换弹中/死亡等守卫，与换弹互斥。

> 脚本：`PlayerInventory.cs`、`InventoryGridBagUI.cs`、`InventorySlotUI.cs`、`ClueNoteReadPanel.cs`、`PickupItem.cs`、`PickupClueNote.cs`、`PlayerHealAction.cs`

 6. 游戏流程管理与 UI 框架

 流程管理器统一 Playing / 暂停 / 结束 / 胜利，切换时处理 `timeScale`、鼠标锁定与输入激活。`UIManager` 维护弹窗栈，各结果面板只订阅状态变化、互不直接引用。

> 脚本：`GameStateManager.cs`、`UIManager.cs`、`PauseMenuUI.cs`、`GameOverUI.cs`、`WinUI.cs`、`InteractionPromptUI.cs`、`PickupToastUI.cs`

 7. 多槽位存档与检查点

 五槽位 JSON 存于可写路径；存档数据结构与「保存/加载时如何把数据写回场景」分离；检查点以关卡交互体打开选槽、二次确认，防误覆盖。

> 脚本：`SaveFileService.cs`、`GameSaveData.cs`、`SaveLoadPanelUI.cs`、`SaveLoadSceneApplier.cs`、`CheckpointStation.cs`、`SaveLoadBootstrap.cs`

 8. 密码门、纸条与关卡门

 密码门：靠近交互、数字键盘、正确开门；输入期间用静态会话类临时关闭视角输入、退出还原。钥匙门查背包是否有钥匙，开门后关碰撞体与 `NavMeshObstacle`，与 AI 通行一致。纸条进背包、右键阅读，与密码门形成信息链。

> 脚本：`PasswordDoorController.cs`、`PasswordDoorSession.cs`、`PasswordKeypadUI.cs`、`PasswordKeypadButtonBinder.cs`、`PickupClueNote.cs`、`LockedDoor.cs`

 9. 尸潮与出口结算

 协程按批次补怪、限制场上同时存活数，避免单帧爆刷；全清后广播可撤离。出口在尸潮清完前不触发胜利。

> 脚本：`HordeWaveManager.cs`、`HordeCrossThresholdTrigger.cs`、`ExitZone.cs`、`WinUI.cs`

 10. 轻量事件中心

 泛型注册表做类型安全的发布/订阅；跨模块用事件中心、同模块强相关仍用 C# 事件。场景重载前清空静态订阅，防悬挂委托。

> 脚本：`EventCenter.cs`、`GameEventPayloads.cs`

 11. 音频与对象池基础

 音效经目录 SO 按键播放；一次性 3D 音效用池。通用 `SimpleObjectPool` / `GameObjectPool` 供音效等高频点使用，可扩展到特效、掉落等。

> 脚本：`AudioManager.cs`、`AudioCatalogSO.cs`、`AudioKeys.cs`、`PooledOneShotAudio.cs`、`SimpleObjectPool.cs`、`GameObjectPool.cs`


 演示流程
1. 基础操作：第三人称移动、视角、切枪/空手、HUD 弹药变化。
2. 探索拾取：走近资源按交互键拾取，飘字提示。
3. 钥匙门：无钥匙有提示，拿到钥匙后开门，门体与寻路障碍同步更新。
4. 战斗：僵尸追击、射击与弹药消耗、击杀倒地；低血负反馈。
5. 背包：打开格子、纸条右键阅读、医疗与互斥条件。
6. 检查点：交互打开五槽位存读档与确认流程。
7. 密码门：可演示输错/对照纸条/开门。
8. 尸潮与胜利：门后触发尸潮、清场后走出口结算。


